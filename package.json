{
  "name": "adaagent",
  "private": true,
  "type": "module",
  "scripts": {
    "install": "npm install --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "dev:all": "concurrently -k -n sidecar,web -c blue,green \"npm run sidecar\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend",
    "sidecar": "node scripts/sidecar.mjs",
    "sidecar:setup": "cd backend && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
