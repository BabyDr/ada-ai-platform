# 复制为 backend/.env，或直接在「仓库根目录」创建 .env（二者都会加载，backend/.env 优先覆盖同名项）

# 智谱 GLM（优先使用）：https://open.bigmodel.cn/ 控制台 API Key
# 可为整条字符串，或 JSON 字符串数组（按顺序拼接、中间无分隔符），例如：
# GLM_API_KEY=["sk-前段","后段"]
GLM_API_KEY=
# 与 GLM_API_KEY 等价，任选其一
# ZHIPU_API_KEY=

# 可选：默认模型（当前端 model_id 不是 glm-* 时）；如 glm-4-plus、glm-4-air、glm-4-flash
# GLM_MODEL=glm-4-flash

# 可选：兼容 OpenAI 的网关地址，默认 https://open.bigmodel.cn/api/paas/v4
# GLM_API_BASE=https://open.bigmodel.cn/api/paas/v4

# —— 以下为 Gemini（未配置 GLM 时使用）；密钥写法同 GLM（整条或 JSON 字符串数组）——
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-2.5-flash
